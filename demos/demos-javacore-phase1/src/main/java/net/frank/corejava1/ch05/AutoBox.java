package net.frank.corejava1.ch05;

public class AutoBox {
	
	/****
	 * java.lang.Integer
	 * int intValue()
	 * 以int的形式返回Integer对象的值
	 * 
	 * static String toString(int i)
	 * 以一个新的String对象的形式返回给定的数值i的十进制表示
	 * 
	 * static String toString(int i, int radix)
	 * 以一个新的String对象的形式返回给定的数值i的radix进制表示
	 * 
	 * static int parseInt(String s)
	 * static int parseInt(String s, int radix)
	 * 返回字符串s表示的整型数值，radix是进制，没有默认是10进制
	 * 
	 * static Integer valueOf(String s);
	 * static Integer valueOf(String s, int radix)
	 * 返回字符串s表示的整型对象，radix是进制，没有默认是10进制
	 * 
	 * java.text.NumberFormat
	 * Number parse(String s)
	 * 返回数值，假设给定的String是一个数值
	 * 
	 * 
	 * 
	 */
	
	
	
	
	
}
